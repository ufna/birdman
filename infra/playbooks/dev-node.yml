---
# Дев-нода birdman (итерация 0): каталоги, бинарь агента, конфиг, GHCR-токен,
# UFW-порты дедиков, unit-заготовка. Требует env BIRDMAN_GHCR_TOKEN (см. infra/README.md).
# containerd НЕ ставим: на дев-боксе он уже есть (docker); прод-роль — specs/ops.md §4.
- name: birdman dev node — agent v0
  hosts: birdman_dev
  become: true
  roles:
    - birdman_agent_dev
