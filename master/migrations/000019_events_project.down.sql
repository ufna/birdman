-- Откат проектного измерения событий (эпик #968, шаг 1).
-- Данные атрибуции теряются безвозвратно: восстановить их можно только
-- повторным бэкфиллом из живых ссылок, и только для тех строк, чьи сущности
-- ещё существуют.
drop index if exists events_project_ts_idx;
alter table events drop column if exists project_id;
